# khoutaibi.github.io

My personal website - [khoutaibi.github.io](https://khoutaibi.github.io)

Built with Jekyll and the [Academic Pages](https://github.com/academicpages/academicpages.github.io) template, served by GitHub Pages directly from the `main` branch (classic Pages build, no Actions workflow needed). Dark theme by default, with a light/dark toggle in the header.

## Editing

- About page and journey timeline: `_pages/about.md`
- Portfolio: add a markdown file in `_portfolio/`
- Repositories page: `_pages/repositories.md`
- Header navigation: `_data/navigation.yml`
- Author sidebar (bio, links): the `author` block in `_config.yml`
- Profile photo: replace `images/profile.png`
- CV: fill `_pages/cv.md`, then re-enable the CV entry in `_data/navigation.yml`

The previous al-folio version of the site is preserved on the `al-folio-version` branch.
